# AI Research Environment

## Quickstart

```bash
git clone <your-repo>
cd ai-research-env

make dev          # Create and initialize environment
cp .env.example .env

make test         # Run tests
make lint         # Run black, isort, flake8
