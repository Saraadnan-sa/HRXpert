name: ai-env
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.11
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=11.8
  - pip
  - pip:
      - transformers
      - scikit-learn
      - mlflow
      - black
      - isort
      - flake8
      - pre-commit
